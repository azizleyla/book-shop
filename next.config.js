/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "bookstoreaze-001-site1.etempurl.com",
  
    ],
  },
}

module.exports = nextConfig
